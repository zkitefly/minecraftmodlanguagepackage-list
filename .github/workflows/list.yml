name: Update list

on: 
  push:
    branches: [main]
    paths: 
      - '.github/workflows/list.yml'
      - 'l.sh'
  schedule:
    - cron: '0 */2 * * *'

jobs:
  update_resources:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name:  install jq & set time & tree
        shell: bash
        run: |
          sudo apt install jq
          jq --version
          sudo timedatectl set-timezone 'Asia/Shanghai'
      - name: check update
        id: check_updata
        shell: bash
        run: |
          echo '========================== 检查区 ==========================='
          wget --no-dns-cache --random-wait --continue -d -T 60 -t 30 --no-warc-digests -O latest.txt "https://api.github.com/repos/CFPAOrg/Minecraft-Mod-Language-Package/releases/latest"
          if md5sum -c --quiet 'md5.md5'; then
            # No changes
            echo 'changed=false' >> $GITHUB_OUTPUT
          else
            # Changes detected
            echo 'changed=true' >> $GITHUB_OUTPUT
            md5sum latest.txt > md5.md5
          fi
          echo '============================================================'
      - name: Fetch list
        if: steps.check_updata.outputs.changed == 'true'
        shell: bash
        run: |
          # 获取 Latest GitHub Releases 名称 & 资源地址
          name=$(cat "latest.txt" | jq -r ".name")
          cat "latest.txt" | jq -r ".zipball_url" > d.txt
          # 获取的 target_commitish  前1-7个字符
          targetcommitish=$(cut "latest.txt" -c1-7 | jq -r ".target_commitish")
          # 
          ./l.sh
          git add -A
          git commit -m "${name}(`date '+%Y-%m-%d %H:%M:%S'`)$GITHUB_RUN_ID
          
          GitHub Action: https://github.com/${{ github.repository }}/actions/runs/$GITHUB_RUN_ID
            "
      - name: Push changes
        if: steps.check_updata.outputs.changed == 'true'
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}